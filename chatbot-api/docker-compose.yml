version: '3.8'

services:
  api:
    build: .
    container_name: chatbot-api
    ports:
      - "8000:8000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    env_file:
      - .env
    restart: unless-stopped